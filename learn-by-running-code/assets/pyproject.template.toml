[project]
name = "{{PROJECT_SLUG}}"
version = "0.1.0"
description = "{{ONE_SENTENCE_GOAL}}"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  {{DEPENDENCIES}}
]

[tool.uv]
package = false

# v0.7.0 验证记录

日期：2026-09-17。

## 确定性回归

```bash
python3 -B -m unittest discover -s learn-by-running-code/scripts -p test_validate_repos.py -v
node scripts/validate-skills.mjs
```

从 skills 仓库根目录运行：26 项 unittest 通过；合集识别 5 个 skill。

覆盖学习模式原契约、非默认 Python 声明、项目无需 Python/uv/章节目录、缺文件/缺标题/空正文/模板标记/错误编码、CLI 返回值、只读边界与四个项目模板填充后的结构一致性。测试中的 uv.lock 是明确标记的结构测试文本，不是真实生成锁文件，也不代表依赖已解析。

## 实际对话探测

使用独立 Agent 加载当前 SKILL，执行一条两轮、只读的 SFT 对话，不提供隐藏的判定答案，不授权文件写入或训练。

1. 输入：“我想做 SFT，你带我做一下。”
   - 实际先询问理解概念还是可用模型、已有基础、模型/数据/代码和算力。
   - 没有擅自生成课程、选定配方或开始训练；说明后续确认费用和执行边界。
2. 输入：明确要做中文客服项目，已有本地对话、想基于 TRL、有单张 24GB 显卡，尚未提供路径。
   - 实际选择 project，不重新问模式；继续问业务/输出、脱敏样本、路径/数据许可、预算/时间/下载边界与协作方式。
   - 建议先做数据检查和基线，再讨论小样本真实训练、重载和对比，等待具体计划确认。

这验证了一个两轮澄清路径，不等于全部 12 个行为规格已执行，也不证明 Agent 能端到端完成任意项目。没有训练模型、运行生成项目或付费调用。

## 审阅发现与修复

静态审阅发现学习校验器将 Python 3.12 固定为唯一值，与允许确认其他版本的说明不一致；已添加显式预期版本参数及回归测试。项目 README 空正文通过，以及学习模式空 README/AGENTS 绕过内容检查的问题也已修复并纳入回归。文档持续区分结构通过、实际运行和最终验收。
